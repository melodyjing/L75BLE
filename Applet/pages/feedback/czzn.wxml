<!--pages/feedback/czzn.wxml-->
<view class="wrapper">
  <view class="list">
    <view class="line" wx:for="{{list}}" data-guide_id="{{item.id}}"  bindtap="detail">
      {{index+1}}.{{item.title}}
    </view>
  </view>
</view>